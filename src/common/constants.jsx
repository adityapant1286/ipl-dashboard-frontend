export const BK = {
    HOST: "http://localhost:8080",
    HEADERS: {
        DEFAULT: {
            'Content-Type': 'application/json'
        }
    },
    ENDPOINTS: {
        TEAMS: "/teams",
        MATCHES: "/matches"
    }
};